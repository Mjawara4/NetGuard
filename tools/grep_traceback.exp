#!/usr/bin/expect -f
set timeout 45
spawn ssh root@74.208.167.166 "cd ~/NetGuard && docker compose logs backend --tail 200 | grep -A 20 'Internal Server Error'"
expect {
  "password:" {
    send "DCaoY6yl\r"
  }
}
expect eof
