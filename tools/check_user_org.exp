#!/usr/bin/expect -f
set timeout 60
spawn ssh root@74.208.192.189 "cd ~/NetGuard && docker compose exec db psql -U postgres -d netguard -c \"SELECT email, organization_id FROM users WHERE email = 'mjawara4@gmail.com';\""
expect "password:"
send "KFh6gyBj\r"
expect eof
