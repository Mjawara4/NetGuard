#!/usr/bin/expect -f
set timeout 120
spawn ssh root@74.208.192.189 "cd ~/NetGuard && docker compose exec backend alembic upgrade head && docker compose exec backend python seed_user.py"
expect "password:"
send "KFh6gyBj\r"
expect eof
