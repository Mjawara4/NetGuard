#!/usr/bin/expect -f
set timeout 30
spawn ssh root@74.208.167.166 "docker exec netguard-db-1 psql -U postgres -d netguard -c 'SELECT name, ip_address, wg_ip_address FROM devices;'"
expect {
  "password:" {
    send "DCaoY6yl\r"
  }
}
expect eof
