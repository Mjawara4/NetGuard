#!/usr/bin/expect -f
set timeout 60
spawn ssh root@74.208.192.189 "cd ~/NetGuard && docker compose exec db psql -U postgres -d netguard -c \"SELECT key, is_active FROM api_keys WHERE key = 'agent-secret-key-123';\""
expect "password:"
send "KFh6gyBj\r"
expect eof
