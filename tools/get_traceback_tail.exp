#!/usr/bin/expect -f
set timeout 45
spawn ssh root@74.208.167.166 "cd ~/NetGuard && docker compose logs backend | grep -A 10 'WireGuardService.generate_mikrotik_script' | tail -n 10"
expect {
  "password:" {
    send "DCaoY6yl\r"
  }
}
expect eof
