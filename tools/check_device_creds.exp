#!/usr/bin/expect -f
set timeout 30
spawn ssh root@74.208.167.166 "docker exec netguard-db-1 psql -U postgres -d netguard -c \"SELECT ip_address, ssh_username, ssh_password, ssh_port, device_type FROM devices WHERE ip_address='10.13.13.2';\""
expect {
  "password:" {
    send "DCaoY6yl\r"
  }
}
expect eof
