#!/usr/bin/expect -f
set timeout 60
spawn ssh root@74.208.192.189 "cd ~/NetGuard && docker compose logs monitor-agent --tail 50 && docker compose exec db psql -U postgres -d netguard -c 'SELECT name, ip_address, device_type FROM devices;'"
expect "password:"
send "KFh6gyBj\r"
expect eof
