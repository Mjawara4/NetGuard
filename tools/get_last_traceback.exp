#!/usr/bin/expect -f
set timeout 45
spawn ssh root@74.208.167.166 "cd ~/NetGuard && docker compose logs backend | grep -A 50 'Traceback (most recent call last)' | tail -n 50"
expect {
  "password:" {
    send "DCaoY6yl\r"
  }
}
expect eof
